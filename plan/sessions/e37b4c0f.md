# Session — e37b4c0f — Phase 5 AI

## Completed This Session

- **Phase 5 — Ask tab** (`app/(tabs)/ask.tsx`): full implementation — chat-style UI with question bubbles, AI answer bubbles, tappable note ID chips linking to `/note/[id]`, loading spinner, empty state (PR #12, merged)
- Carried over leftover Phase 4 config: iOS mic/speech permissions in `app.json`, Android audio permissions, `bundleIdentifier` (also in PR #12)
- Marked BUG-002 and BUG-003 as RESOLVED (confirmed by user, PRs #9/#10 already merged)
- Logged and fixed **BUG-005** — `ExpoSpeechRecognition` hard crash on launch: replaced static import with `try/require` guard in `useVoiceRecorder.ts` so the app degrades gracefully when the native module isn't linked (PR #13, merged)
- Logged and fixed **BUG-006** — `/run` skill crashing with `node ios`: added `.claude/commands/run.md` project skill with the correct `npm run ios` command (PR #14, open)
- Created **`pr-testing` skill** (`.claude/commands/pr-testing.md`): runs merge conflict check + `tsc` + `jest` before any PR is submitted (also in PR #14)
- Logged **BUG-007** — `expo-speech-recognition` native module still not linked after `npm run ios` even with clean build; live transcription permanently disabled
- Renamed `session-handoff` skill to **`session-manager`** and updated framing to reflect session lifecycle management (also in PR #14)

## Prior Next Steps — Status

| Task | Status |
|------|--------|
| Set up API key for real AI categorization (`.env`) | ❌ Not started |
| Phase 4 — Voice recording screen | ✅ Done (merged PR #8) |
| Phase 5 — Ask tab | ✅ Done (merged PR #12) |
| Phase 6 — Recap tab | ❌ Not started |
| Run tests: `npx jest --no-coverage` | ✅ Done (33/33 passing) |
| Type-check: `npx tsc --noEmit` | ✅ Done (clean) |

## Next Steps

> Run these yourself to continue:

1. **Merge PR #14** (`fix/bug-006-run-skill`) — adds `run.md` + `pr-testing` skill + BUG-007 log; then clean up: `git checkout main && git pull origin main && git branch -d fix/bug-006-run-skill && git push origin --delete fix/bug-006-run-skill`
2. **Fix BUG-007** — `expo-speech-recognition` not linking. Try a full clean prebuild: `npx expo prebuild --clean && npm run ios`. If it still fails, the module may need manual Podfile configuration or is broken under SDK 56 — check the `expo-speech-recognition` GitHub issues for SDK 56 compatibility.
3. **Fix BUG-004** — speech recognizer fails to initialize on simulator. Once BUG-007 is resolved and the module is linked, test on a physical device to confirm whether transcription works (simulator limitation vs real bug). If simulator-only, add `requiresOnDeviceRecognition: true` to `ExpoSpeechRecognitionModule.start()`.
4. **Phase 6 — Recap tab** (`/(tabs)/recap`): Today / This Week / This Month period buttons, `generateRecap` from `useAI`, `RecapCard` with summary + bullet highlights.
5. **Set up API key** — create `.env` with `EXPO_PUBLIC_AI_PROVIDER=claude` and `EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-...` for live AI categorization and Ask tab.
6. To run tests: `npx jest --no-coverage`
7. To type-check: `npx tsc --noEmit`
8. To run simulator: `npm run ios`

## Open Bugs

**BUG-004** — Speech recognizer fails to initialize, no transcript captured
- Status: BACKLOG
- Try on physical device first; may be simulator-only limitation

**BUG-007** — expo-speech-recognition native module not linked after `npm run ios`
- Status: BACKLOG
- Try: `npx expo prebuild --clean && npm run ios`
