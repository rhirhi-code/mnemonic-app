# Session Handoff — 300c7785

## Completed This Session

- Fixed BUG-001: documented iOS Simulator tunnel workaround in `plan/run-simulator-instructions.md`
- Fixed missing root redirect — added `app/index.tsx` so app lands on the Notes tab instead of the not-found screen
- Verified the app in the iOS Simulator: three tabs present, no crash on launch
- Pushed all Phase 0 + Phase 1 work to a new private GitHub repo (`rhirhi-code/mnemonic-app`)
- Removed `.claude/` and `.vscode/` from version control, added both to `.gitignore` (PR #1, PR #2)
- Moved `plan/session-handoff.md` into `plan/sessions/` subdirectory (PR #3)
- Added PR workflow instructions to `AGENTS.md` — branch → PR → owner merges → delete both branches
- Created `/session-handoff` skill at `.claude/commands/session-handoff.md`

## Prior Next Steps — Status

| Task | Status |
|------|--------|
| Implement `ai/providers/claude.ts` | ❌ Not started |
| Implement `ai/index.ts` provider factory | ❌ Not started |
| Implement `hooks/useNotes.ts` | ❌ Not started |
| Implement `hooks/useCategories.ts` | ❌ Not started |
| Implement `hooks/useAI.ts` | ❌ Not started |

## Next Steps

> Run these yourself to continue:

1. Start the next session with `/session-handoff start` to orient Claude on where things left off
2. Phase 2 is next — AI integration. Begin with `ai/providers/claude.ts` and `ai/index.ts`
3. To run the simulator: `cd /Users/rhiannon/code/mnemonic && npm run ios -- --tunnel`
4. To run tests: `cd /Users/rhiannon/code/mnemonic && npx jest --no-coverage`
5. To type-check: `cd /Users/rhiannon/code/mnemonic && npx tsc --noEmit`

## Open Bugs

- **BUG-001** — iOS Simulator times out opening Expo Go URL — **Resolved**, see `plan/run-simulator-instructions.md`
